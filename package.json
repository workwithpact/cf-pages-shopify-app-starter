{
  "name": "cf-shopify-app-starter",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@shopify/polaris": "^7.5.0",
    "@testing-library/jest-dom": "^5.14.1",
    "@testing-library/react": "^12.0.0",
    "@testing-library/user-event": "^13.2.1",
    "@types/jest": "^27.0.1",
    "@types/node": "^16.7.13",
    "@types/react": "^17.0.20",
    "@types/react-dom": "^17.0.9",
    "dotenv": "^11.0.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "6",
    "react-scripts": "5.0.0",
    "typescript": "^4.4.2",
    "web-vitals": "^2.1.0",
    "wrangler": "^0.0.7"
  },
  "scripts": {
    "start": "yarn write-env && npx wrangler pages dev -- react-scripts start",
    "build": "[ -e \"env.json\" ] || echo '{}' > env.json && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "write-env": "[ -e \"env.json\" ] || echo '{}' > env.json"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
